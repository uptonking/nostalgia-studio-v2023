import { css } from '@linaria/core';
import { themed } from '@pgd/ui-tokens';

export const demoPreviewDefaultContainerCss = css`
  max-width: ${themed.spacing.rem.n96};
  max-height: ${themed.spacing.rem.n48};
`;
